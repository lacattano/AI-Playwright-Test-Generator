# ── Cache directories ──────────────────────────────────────────────
.mypy_cache/
.ruff_cache/
.pytest_cache/
__pycache__/
htmlcov/
.venv/
.uv-cache/

# ── Lockfiles (token monsters) ────────────────────────────────────
uv.lock

# ── Coverage artifacts (regenerated by pytest --cov) ──────────────
coverage.xml
coverage.json
.coverage

# ── Benchmark artifacts ───────────────────────────────────────────
bench_*.json

# ── Runtime evidence & temporary ──────────────────────────────────
evidence/
.tmp/
tmp_*.py
*.log
bench_*

# ── Scratch & experimental ────────────────────────────────────────
scratch/

# ── MCP Memory Bank (regenerated) ─────────────────────────────────

# ── Build/Node ────────────────────────────────────────────────────
node_modules/
build/
dist/

# ── Git ───────────────────────────────────────────────────────────
.git/

# ── Playwright artifacts ──────────────────────────────────────────
test-results/
playwright-report/
blob-report/
playwright/.cache/

# ── Streamlit config ──────────────────────────────────────────────
.streamlit/

# ── Compiled/Binary ──────────────────────────────────────────────
*.pyc
*.py[o]
*.so
*.egg-info/

# ── Context window reduction ──────────────────────────────────────
# Exclude large doc directories from file listing (kept on disk)
docs/reference/
docs/archived/
docs/session_*.md
docs/test_suite_audit_*.md
docs/private/


# ── Generated test output (many files, regenerated) ───────────────
generated_tests/test_*.py

# ── Test artifacts ────────────────────────────────────────────────

