# Shot List — Short Demo Video

shot01_hook.png (0–5s)
- Full app title, brief UI pan; split-screen showing Streamlit UI (left) and CLI (right); overlay text: "From user story to test in under a minute"

shot02_paste_story.png (5–10s)
- Sidebar/base-url; paste user story textarea highlighted (also show equivalent CLI command input in the right pane)

shot03_acceptance.png (10–15s)
- Close-up on acceptance criteria lines

shot04_generate_spinner.gif (15–20s)
- Click Generate in Streamlit (or press Enter/run command in CLI); record spinner/console output; render as 2x speed if slow

shot05_code_zoom.png (20–30s)
- Code tab open in Streamlit; show CLI output snippet with file path on the right; zoom into selectors: get_by_placeholder, get_by_role, get_by_text

shot06_run_ui.gif (30–34s)
- Click Run in Streamlit or show CLI invoking pytest; brief browser execution or test output pane

shot07_results.png (34–36s)
- Test results summary: "2 passed" (show both UI result pane and CLI pytest summary)

shot08_reports.png (36–40s)
- Show generated reports: open HTML report in browser (preview), show JSON report snippets and thumbnails of screenshots; click through a screenshot

shot09_export_cta.png (40–45s)
- Show export buttons (Python / JSON / HTML); overlay CTA text and GitHub link

Notes:
- Save assets into assets/demo_shots/ with the same filenames
- Use consistent font for captions; keep them legible on phone screens
- Prefer shots that read well on a phone (crop to 16:9 or vertical variants)